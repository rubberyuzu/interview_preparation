#include <iostream>

using namespace std;


int main(){
	int W, N, K;
	cin >> W;
	cin >> N >> K;
	int i = 0;
	int width[N];
	int value[N];
	for (i = 0; i < N; ++i){
		cin >> width[i];
		cin >> value[i];
	}
 // to be solved //



}